package nz.amldock.suspiciousactivity;

/**
 * What the suspicion is about. A TRANSACTION carries a monetary amount; an ACTIVITY does not.
 */
public enum SuspicionType {
    ACTIVITY,
    TRANSACTION
}
