VITE_API_BASE=/api

# AddressFinder address-autocomplete widget key. Public and domain-restricted, so it belongs in
# the browser bundle — but it only returns results from a host whitelisted in the AddressFinder
# dashboard (add localhost:5173 for local work). Leave blank to fall back to manual address entry.
VITE_ADDRESSFINDER_KEY=
